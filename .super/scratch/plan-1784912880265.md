No hay skills instaladas en el sistema. Déjame buscar si hay un archivo de skill "UI Pro Max" en el proyecto.No encontré ninguna skill llamada "UI Pro Max" en el proyecto. Verifiqué:

- `skill list` → 0 skills instaladas
- Búsquedas de texto (`UI Pro Max`, `ui-pro-max`, `SKILL.md`, `skill`) → 0 coincidencias en el workspace

No voy a inventar el contenido de esa skill. Para proceder necesito una de estas opciones:

1. **Instalar la skill** — Si existe en un repo de GitHub, dame el `owner/repo` y la instalo con `skill install`.
2. **Indicar la ruta** — Si el archivo `SKILL.md` (o similar) ya está en el proyecto pero con otro nombre, dime dónde está y lo leo.
3. **Pegar el contenido** — Si la tienes a mano, pega el texto de la skill y la aplico.

¿Cuál de las tres prefieres? Mientras tanto, si quieres, puedo leer la pantalla de Movimientos actual (`lib/features/movimientos/`) para ir preparando el contexto del plan.